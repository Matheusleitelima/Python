 

palavra = 'Google'

for letra in palavra:
    print(letra)



for letras in 'Google':
    print(letras)


for letra in palavra:
    print(f'{letra} estar dentro da palavra {palavra}')


#Imprimir a palavra toda

palavra = 'Google'

for letra in palavra:
    print (palavra)


#Para repetir a palavra em um determinado numero de vezes

palavra = 'teclado'

for _ in range(4):
    print (palavra)
